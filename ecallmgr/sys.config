[{kernel, [
    {logger, [
        {handler, default, logger_std_h,
            #{formatter => {logger_formatter, #{}}},
            level => info,
            config => #{file => "/var/log/kazoo/ecallmgr.log"}
        }
    ]}
]}].